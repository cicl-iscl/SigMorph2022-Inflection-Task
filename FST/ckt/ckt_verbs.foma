read lexc ckt_verbs.lexc
define morph;

source ckt_verbs.xfst
define phon;

define buffer  A ->  э || _ Cns+ э,, A ->  а || _ Cns+ а;

define Cleanup "^" -> 0;

regex morph .o. phon .o. buffer .o. Cleanup;

save stack ckt_verbs.bin
